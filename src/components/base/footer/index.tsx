import { ReactElement } from 'react';

import styles from './index.module.scss';

const Footer = (): ReactElement => {
  return <header className={styles.header}>This is Footer</header>;
};

export default Footer;
